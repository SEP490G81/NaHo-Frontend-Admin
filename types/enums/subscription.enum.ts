export enum SubscriptionTier {
    FREE = "FREE",
    BASIC = "BASIC",
    PREMIUM = "PREMIUM",
}

export enum SubscriptionStatus {
    ACTIVE = "ACTIVE",
    EXPIRED = "EXPIRED",
    CANCELLED = "CANCELLED",
}
